export enum TABLE_COL_TYPE {
    DATE = 'date',
    TEXT = 'text',
    MONEY = 'money',
    HYPERLINK = 'hyperlink',
    EVENT = 'event',
    DATE_TIME = 'date_time',
    NUMBER = 'number',
    ACTIVE = 'active',
    SELECT = 'select',
    MUTIL_SELECT = 'mutil_select',
    ICON="icon", 
    STYLE="style", 
    SHOW_INPUT_WHEN_SELECT = 'show_input_when_select',
    SHOW_INPUT_DATE_WHEN_SELECT = 'show_input_date_when_select',
    COMMENT = 'comment'
}

export enum TABLE_EVENT_TYPE {
    SORT = 'sort',
    SELECT = 'select',
    PAGE_CHANGE = 'pageChange',
    FILTER = 'filter',
    EVENT = 'event',
    HYPERLINK = 'hyperlink',
    CLICK_ROW = 'clickRow'
}

export enum TABLE_ACTION_TYPE {
    DELETE = 'delete',
    EDIT = 'edit',
    VIEW = 'view',
}

